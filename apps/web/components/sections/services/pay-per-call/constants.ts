/**
 * Shared layout and design tokens for Pay Per Call sub-gateway pages.
 * Matches parent page rhythm: max-w-7xl, py-20 md:py-24, alternating bg-background / bg-muted/30.
 */
export const SECTION_PADDING = 'max-w-7xl mx-auto py-20 md:py-24 px-4 sm:px-6 md:px-8';

export type CallGatewayAccent = 'mint' | 'lilac' | 'peach' | 'sky';

export type CallLogicBgVariant = 'search_social' | 'live_transfer' | 'offline_media';
